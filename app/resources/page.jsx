import Resources from '@/components/modules/resources/Resources';

export const metadata = { title: 'Resources — Dominion Edge Holdings' };

export default function ResourcesPage() {
  return <Resources />;
}
